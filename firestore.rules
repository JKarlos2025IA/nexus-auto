rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // Ventas collection - require authentication
    match /ventas/{ventaId} {
      allow read, write: if request.auth != null;
    }
  }
}
